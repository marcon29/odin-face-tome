<% if action_name == "show" %>
    <%= render "posts/post", post: @post, comments: @post.comments, likes: @post.likes, like: @like %>
<% else %>
    <%= render "posts/post_form", post: @post %>
<% end %>

